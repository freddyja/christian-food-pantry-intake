# Optional remote libSQL / Turso database (recommended on Vercel so visits persist).
# Locally, the app uses a SQLite file at data/pantry.db and seeds sample households.
# TURSO_DATABASE_URL=libsql://your-db.turso.io
# TURSO_AUTH_TOKEN=

# Soft PIN for the Admin screen. Demo default is 1234.
ADMIN_PIN=1234
